% !TeX root = ../main.tex

\chapter{算法整合、误差分析与复杂度分析}
\label{chap:algorithm-theory}

第4章给出三维自适应参数——频率权重 $w_k(i)$、节点正则系数 $\tau(i)$、跳距预算配额 $k_i^{(l)}$——各维度独立设计，尚未形成统一的可执行流程。本章在此基础上完成最后三步：以LARS为核心求解器将三维参数整合为端到端交替优化算法（§\ref{sec:ch5-lars}）；通过$\sigma$-谱相似性给出稀疏化后的误差分析与适用条件（§\ref{sec:ch5-spectral}）；分析推理与训练各阶段复杂度并说明成立条件（§\ref{sec:ch5-time}）。

本章整体框架如图~\ref{fig:ch5-overview} 所示。

% ---- 图~\ref{fig:ch5-overview}~占位符 ----
\begin{figure}[htbp]
  \centering
      \includegraphics[width=1\linewidth]{5-1.pdf}
  % \setlength{\fboxsep}{1.2em}
  % \fbox{\parbox{0.88\textwidth}{\small\centering
  %   \textbf{【占位图~\ref{fig:ch5-overview}~】}\\[0.6em]
  %   图题：第5章整体框架图。表达内容：从三维参数输入（$\tau(i)$、$k_i^{(l)}$、$\mathcal{R}_l(i)$）→LARS端到端算法（算法5.1/5.2，输出$\Theta^{\text{fixed}}$）→谱保持理论（式~\eqref{eq:ch5-tau-grace}、式~\eqref{eq:ch5-budget-lagrange}，参数初始化参考）→复杂度分析（$O(n\bar{k}d)$及其适用条件）的四层纵向流程，各层以箭头标注数据流向。
  % }}
  \caption{第5章整体框架图：三维参数整合→LARS算法→误差分析→复杂度}
  \label{fig:ch5-overview}
\end{figure}

%%============================================================
\section{LARS分层稀疏求解与交替优化}
\label{sec:ch5-lars}

第4章三类自适应参数（$w_k(i)$、$\tau(i)$、$k_i^{(l)}$）目前各自独立，尚未形成统一求解流程。本节以LARS\cite{Efron2004LARS}（Efron等，2004）为核心求解器：LARS沿Lasso解路径生成分段线性的稀疏解序列，以$\tau(i)$为正则系数，令 \texttt{max\_iter} 取 $k_i^{(l)}$ 作为预算截断上限，从而将三维参数直接落地为求解器接口。具体地，候选集按跳距划分为环层 $\mathcal{R}_l(i)$（第~\ref{subsec:ch5-candidateset}~节），各层顺序执行残差级联LARS；完整端到端流程见第~\ref{subsec:ch5-mainloop}~节算法~\ref{alg:ch5-main}，收敛性分析见第~\ref{subsec:ch5-convergence}~节。

\subsection{分层LARS的候选集构造与方案选定}
\label{subsec:ch5-candidateset}

候选集定义为前$L$跳邻居并集$C_i=\bigcup_{l=1}^{L}\mathcal{R}_l(i)$（环层$\mathcal{R}_l(i)$两两不相交）。大图训练时可采用“前 $K_{\mathrm{keep}}$ 跳完整保留 + 第 $K_{\mathrm{sample}}$ 跳起分层采样”的工程裁剪策略，其中 $K_{\mathrm{keep}}$ 与 $K_{\mathrm{sample}}$ 为预先设定的裁剪超参数。

作为对照，可先考虑共享目标方案：记 $\Omega_i:=H_i^*$ 为教师/基准 GNN 在节点 $i$ 处的目标表示，最直接的做法是对每个环层 $\mathcal{R}_l(i)$ 各自独立求解：
\begin{equation}
\theta_i^{(l)} = \operatorname*{arg\,min}_{\theta\in\mathbb{R}^{|\mathcal{R}_l(i)|}}
\left\|\theta^\top\tilde{\Phi}_{\mathcal{R}_l(i)} - \Omega_i\right\|^2 + \tau(i)\|\theta\|_1
\label{eq:ch5-shared-target}
\end{equation}

每层均以同一 $\Omega_i$ 为拟合对象，存在对同一目标的重复逼近与信息冗余，故本文不采用该路径（以下称方案~1）。

方案2（残差级联）将各层拟合对象改为逐步更新的残差，对应子问题为
\begin{equation}
  \theta_i^{(l)} = \operatorname*{arg\,min}_{\theta\in\mathbb{R}^{|\mathcal{R}_l(i)|}}
\left\|\theta^\top\tilde{\Phi}_{\mathcal{R}_l(i)} - r_i^{(l-1)}\right\|^2 + \tau(i)\|\theta\|_1
  \label{eq:ch5-tau-full}
\end{equation}

实现上令 \texttt{max\_iter} 取 $k_i^{(l)}$ 作为截断上限，并按
$r_i^{(l)}:=r_i^{(l-1)}-(\theta_i^{(l)})^\top\tilde{\Phi}_{\mathcal{R}_l(i)}$
更新残差，最终拼接得到 $\theta_i$。与方案1相比，方案2以“逐层拟合残差”替代“逐层重复逼近同一目标”，因此更符合第4章第~\ref{sec:ch4-hop}~节所体现的“近跳优先、远跳递减”预算动机。需要说明的是，这里仅给出工程合理性解释，不将其表述为整体 Lasso 支撑集一致性的严格结论。故本文最终采用方案2。

% ---- 图~\ref{fig:ch5-lars-compare}~占位符 ----
\begin{figure}[htbp]
  \centering
      \includegraphics[width=1\linewidth]{5-2.pdf}
  % \setlength{\fboxsep}{1.2em}
  % \fbox{\parbox{0.88\textwidth}{\small\centering
  %   \textbf{【占位图~\ref{fig:ch5-lars-compare}~】}\\[0.6em]
  %   图题：共享目标方案与残差级联方案对比。表达内容：左侧给出“共享目标”路径，显示各环层均直接拟合同一目标 $\Omega_i$；右侧给出“残差级联”路径，显示第 $l$ 层以 $r_i^{(l-1)}$ 为拟合对象并逐层更新残差。图中用箭头对比两种方案的信息流向，并标注“前者存在重复逼近，后者更符合分层预算动机”。 
  % }}
  \caption{共享目标与残差级联两种分层 LARS 方案对比}
  \label{fig:ch5-lars-compare}
\end{figure}

\subsection{完整交替优化算法}
\label{subsec:ch5-mainloop}




$\tau(i)$ 由算法~\ref{alg:ch4-tau-compute}（手工特征路径）生成，数值范围 $[\tau_{\min},\tau_{\text{base}}]$，直接作为第5章主循环的节点级正则系数输入。



\begin{algorithm}[tp]
\caption{MSAS-GNN 三维自适应稀疏分解主循环（完整交替优化版，主实验口径）}
\label{alg:ch5-main}
\begin{algorithmic}[1]
\setlength{\algorithmicindent}{2em}
\REQUIRE 图 $G=(V,E,X)$，$X\in\mathbb{R}^{n\times D}$；教师/基准 GNN 离线输出的目标中间表示矩阵 $H^*\in\mathbb{R}^{n\times d}$；外部给定的节点级正则系数 $\{\tau(i)\}_{i\in V}$（来自第4章第4.2节）；分层预算配额 $\{k_i^{(l)}\}$（优先由第4章第4.3节预计算并输入；若缺省则需同时给定$\lambda_{\text{gap}}$与$\{k_i^{\text{total}}\}$以便按第4章规则在线生成）；预训练参数 $W_\phi^*$；超参数 $L$，$T_{\text{epoch}}$，$T_W$，$\eta_W$，$q$
\ENSURE 稀疏权重矩阵 $\Theta=\{\theta_i\}_{i\in V}$，特征变换参数 $W_\phi$
\STATE \textbf{阶段一：候选集与环层构建（一次性预处理）}
\FORALL{$i\in V$}
  \STATE $\mathcal{R}_l(i) \leftarrow \{j\in V \mid d_G(i,j)=l\}$，$l=1,\ldots,L$（按跳距分环层）
  \STATE $C_i \leftarrow \bigcup_{l=1}^{L}\mathcal{R}_l(i)$（保留完整 $L$-hop 邻域）
\ENDFOR
\STATE \textbf{阶段二（可选）：分层跳距预算在线生成}
\STATE 若输入中已直接给出 $\{k_i^{(l)}\}$，则跳过本阶段；否则按第4章第4.3节的分配规则，由$\lambda_{\text{gap}}$与$\{k_i^{\text{total}}\}$在线生成 $\{k_i^{(l)}\}$。
\STATE \textbf{阶段三：full-batch 交替优化}
\STATE $W_\phi \leftarrow W_\phi^*$；$\Omega \leftarrow H^*$；$\Theta \leftarrow 0$（$\Theta\in\mathbb{R}^{n\times n}$，与 $\tilde{\Phi}\in\mathbb{R}^{n\times d}$ 配套，使 $\hat{H}=\Theta^\top\tilde{\Phi}$ 为全图节点表示）
\FOR{$e=1$ \textbf{to} $T_{\text{epoch}}$}
  \STATE $\tilde{\Phi} \leftarrow \ell_2\text{-row-normalize}\!\bigl(\phi(X;W_\phi)\bigr)$（若 $\phi$ 为单层线性变换，则该步复杂度为 $O(nDd)$）
  \STATE \textbf{Phase $\Theta$（固定 $W_\phi$，对全体节点执行分层 LARS）：}
  \FORALL{$i\in V$}
    \STATE $r_i^{(0)} \leftarrow \Omega_i$
    \FOR{$l=1$ \textbf{to} $L$}
      \STATE $\theta_i^{[l]} \leftarrow \operatorname{LARS}\!\!\left(\min_\theta\left\|\theta^\top\tilde{\Phi}[\mathcal{R}_l(i),:]-r_i^{(l-1)}\right\|_2^2+\tau(i)\|\theta\|_1\right)\!\Big|_{\mathrm{max\_iter}=k_i^{(l)}}$
      \STATE $r_i^{(l)} \leftarrow r_i^{(l-1)}-(\theta_i^{[l]})^\top\tilde{\Phi}[\mathcal{R}_l(i),:]$
    \ENDFOR
    \STATE $\theta_{i,C_i} \leftarrow \operatorname{Concat}(\theta_i^{[1]},\ldots,\theta_i^{[L]})$
    \STATE $\Theta[C_i,i] \leftarrow \theta_{i,C_i}$；第 $i$ 列在行索引 $j\notin C_i$ 处置 $0$
  \ENDFOR
  \STATE \textbf{Phase $W$（固定 $\Theta$，优化 $W_\phi$）：}
  \FOR{$s=1$ \textbf{to} $T_W$}
    \STATE $\tilde{\Phi} \leftarrow \ell_2\text{-row-normalize}\!\bigl(\phi(X;W_\phi)\bigr)$；$\hat{H} \leftarrow \Theta^\top\tilde{\Phi}$
    \STATE $W_\phi \leftarrow \operatorname{AdamStep}\!\left(W_\phi,\;\nabla_{W_\phi}\tfrac{1}{2}\left\|\hat{H}-\Omega\right\|_F^2;\,\eta_W\right)$
  \ENDFOR
  \IF{$\operatorname{ValMetricStable}(\text{window}=q)$}
    \STATE \textbf{break}
  \ENDIF
\ENDFOR
\RETURN $\Theta,\;W_\phi$
\end{algorithmic}
\end{algorithm}

其中 $\hat{H}=\Theta^\top\tilde{\Phi}\in\mathbb{R}^{n\times d}$ 为全图稀疏近似表示矩阵。由于节点 $i$ 的稀疏系数写入 $\Theta$ 的第 $i$ 列，$\Theta^\top\tilde{\Phi}$ 的第 $i$ 行恰对应节点 $i$ 的近似表示；Phase $W$ 优化全图表示逼近目标 $\frac{1}{2}\|\Theta^\top\tilde{\Phi}-H^*\|_F^2$，保证 $\Theta$ 与 $W_\phi$ 每轮全图同步。

\begin{algorithm}[htbp]
\caption{推理期稀疏固化}
\label{alg:ch5-fixed}
\begin{algorithmic}[1]
\setlength{\algorithmicindent}{2em}
\REQUIRE 训练完成的稀疏权重 $\Theta=\{\theta_i\}_{i\in V}$，特征变换参数 $W_\phi$，图 $G=(V,E,X)$
\ENSURE 固化稀疏权重 $\Theta^{\text{fixed}}$，缓存归一化特征矩阵 $\tilde{\Phi}^{\text{cache}}$（第6章实验接口）
\STATE $\Theta^{\text{fixed}} \leftarrow \Theta$（冻结训练所得稀疏权重，推理期不再更新）
\STATE $\tilde{\Phi}^{\text{cache}} \leftarrow \ell_2\text{-row-normalize}\!\left(\phi(X;\,W_\phi)\right)$（若 $\phi$ 为单层线性变换则复杂度为 $O(nDd)$）
\RETURN $\Theta^{\text{fixed}},\;\tilde{\Phi}^{\text{cache}}$
\end{algorithmic}
\end{algorithm}

两个算法共享同一预算接口：$\tau(i)$与$\{k_i^{(l)}\}$均作为流程输入，其中算法~\ref{alg:ch5-main}消费二者，算法~\ref{alg:ch5-fixed}仅输出$\Theta^{\text{fixed}}$与$\tilde{\Phi}^{\text{cache}}$。




固化后的推理过程为单次稀疏矩阵乘：
\begin{equation}
  \hat{H} = (\Theta^{\text{fixed}})^\top \tilde{\Phi}^{\text{cache}},\quad \text{复杂度 }O(m_{\text{sparse}}\,d) = O(n\bar{k}d)
  \label{eq:ch5-tau-range}
\end{equation}

其中 $m_{\text{sparse}}=\sum_{i\in V}\|\theta_i\|_0=n\bar{k}$ 为全图非零权重总数，$\bar{k}:=\frac{1}{n}\sum_{i\in V}k_i^{\text{total}}$ 为各节点总预算的全图平均值。各阶段完整复杂度口径见第~\ref{sec:ch5-time}~节。

\subsection{优化稳定性与实现说明}
\label{subsec:ch5-convergence}

固定$W_\phi$时，每个环层子问题都对应一个带$\ell_1$正则的凸代理问题；若对该子问题完整执行LARS，则可获得其分段线性路径上的精确解。当前Phase $\Theta$还叠加了候选集限制、残差级联与\texttt{max\_iter}截断，因此整阶段应视为分层近似求解，而不宜表述为“精确追踪整体Lasso解路径”。对外层交替优化，本文仅声称经验稳定，不声称全局收敛；相关现象将在第~6~章训练曲线中验证。从工程角度看，Phase $\Theta$ 的 LARS 求解每步均局部降低目标值，Phase $W$ 采用衰减学习率的随机梯度更新，在合理学习率范围内不会持续增大目标，两者的交替推进在六个数据集上均表现为训练曲线平稳下降\cite{Tseng2001,Razaviyayn2013}。三维参数至此整合为端到端优化闭环，固化后推理接口（式~\eqref{eq:ch5-tau-range}）直接进入第~\ref{sec:ch5-spectral}~节的误差分析。


\section{误差分析与适用条件}
\label{sec:ch5-spectral}\label{idx:theme:specpreserve}

第4章各维度设计均属启发式或条件性设计，尚未定量说明稀疏化后的误差范围。本节以$\sigma$-谱相似性为度量工具，给出多项式谱滤波器的近似误差界，并说明其适用条件。



\subsection{谱相似性与图稀疏化}
\label{subsec:ch5-spectralsim}

稀疏化将原图$G$替换为边数更少的$\hat{G}$，为量化谱结构破坏程度，引入以下定义。

\phantomsection\label{def:ch5-sigmasim}
为量化稀疏子图对原图谱结构的保持程度，本文引入\textbf{$\sigma$-谱相似性}（$\sigma$-spectral similarity）的概念。设 $G$ 与 $\hat{G}$ 为定义在同一节点集 $V$ 上的两个图，其归一化拉普拉斯分别为 $\tilde{\mathcal{L}}_G$ 与 $\tilde{\mathcal{L}}_{\hat{G}}$。若对任意 $x\in\mathbb{R}^n$ 满足：
\begin{equation}
  \frac{1}{\sigma}\,x^\top\tilde{\mathcal{L}}_G x
\;\leq\; x^\top\tilde{\mathcal{L}}_{\hat{G}}x
\;\leq\; \sigma\, x^\top\tilde{\mathcal{L}}_G x,
  \label{eq:ch5-tau-mono}
\end{equation}

则称 $\hat{G}$ 是 $G$ 的 $\sigma$-谱稀疏子图，参数 $\sigma\geq 1$，越接近1谱保持越精确。$\sigma$ 可由精确特征分解（$O(n^3)$，适用于小图）或 Lanczos 代理量 $\widetilde{\sigma}_{\mathrm{proxy}}$ 估计；代理量与严格 $\sigma$ 的偏差在第6章实验中统一报告。


\subsection{多项式谱滤波器的近似误差界}
\label{subsec:ch5-polyfilter}

以下建立多项式谱滤波器的近似误差界（式~\eqref{eq:ch5-tau-grace}）。

设$g:\mathbb{R}\to\mathbb{R}$为一$K_{\text{poly}}$次多项式，则对应的谱滤波器定义为将其作用于归一化拉普拉斯特征值上的矩阵函数：
\begin{equation}
g(\tilde{\mathcal{L}}) \;:=\; g(U\Lambda U^\top) \;=\; U\,\mathrm{diag}(g(\lambda_0),g(\lambda_1),\ldots,g(\lambda_{n-1}))\,U^\top
\label{eq:ch5-gpoly-def}
\end{equation}

本节中，$g(\tilde{\mathcal{L}})$统一以Chebyshev多项式$\{T_k\}_{k=0}^{K_{\text{poly}}}$展开：$g(\tilde{\mathcal{L}})=\sum_{k=0}^{K_{\text{poly}}} c_k T_k(\bar{\mathcal{L}})$，其中重标定拉普拉斯采用
\begin{equation}
\bar{\mathcal{L}}:=\tilde{\mathcal{L}}-I
\label{eq:ch5-barL-def}
\end{equation}

（$\mathrm{spec}(\tilde{\mathcal{L}})\subset[0,2]$，此变换将原图与稀疏图均映射到$[-1,1]$，便于统一Chebyshev展开。）

$K_{\text{poly}}$（多项式滤波器次数）与第3章$K_{\text{eig}}$（Lanczos阶数）须严格区分，不可混用。

\phantomsection\label{prop:ch5-sigma}\label{idx:innov:sigma}\label{idx:theme:spectralsim}
本节的核心结论是：当稀疏图 $\hat{G}$ 满足 $\sigma$-谱相似性条件
\begin{equation}
(1/\sigma)\tilde{\mathcal{L}}_G \;\preceq\; \tilde{\mathcal{L}}_{\hat{G}} \;\preceq\; \sigma\tilde{\mathcal{L}}_G
\label{eq:ch5-sigma-loewner}
\end{equation}

时，多项式谱滤波器的输出误差可被 $\sigma$ 参数化地控制。节点覆盖率、谱能量驱动的保留规则与分层预算，属于后文“从三维设计参数到较小谱扰动”的工程代理分析，不构成本误差界的严格前提。

具体地，若 $g(\tilde{\mathcal{L}})$ 为 $K_{\text{poly}}$ 阶多项式谱滤波器（Chebyshev展开系数为 $\{c_k\}_{k=0}^{K_{\text{poly}}}$，$T_k$ 对应的单项常数为 $C_{T,k}$），则对任意节点特征矩阵 $X\in\mathbb{R}^{n\times D}$，有：
\begin{equation}
  \left\|g(\tilde{\mathcal{L}}_G)X - g(\tilde{\mathcal{L}}_{\hat{G}})X\right\|_F
\;\leq\; 2(\sigma-1)\cdot\left(\sum_{k=0}^{K_{\text{poly}}}|c_k|\,C_{T,k}\right)\cdot\|X\|_F
  \label{eq:ch5-tau-grace}
\end{equation}

该误差界的核心含义是：当稀疏图 $\hat{G}$ 相对原图 $G$ 的谱结构偏差（由参数 $\sigma$ 刻画）越小，输出特征矩阵的偏差也越小。对 GCN（$K_{\text{poly}}=1$）而言，界简化为 $2(\sigma-1)\|X\|_F$。该结论可由算子范数不等式、Chebyshev 多项式的 Lipschitz 性质与三角不等式组合推导得出：将 $g(\tilde{\mathcal{L}}_G)-g(\tilde{\mathcal{L}}_{\hat{G}})$ 展开为各阶 Chebyshev 项之差，对每阶利用 $\sigma$-谱相似性条件控制算子范数偏差，再对各阶求和即得\cite{Chung1997,Shuman2013GSP}。第6章实验在多个数据集上对 $\sigma$ 进行了实测验证。

此即式~\eqref{eq:ch5-tau-grace}，其中右端括号内的常数$\sum_k|c_k|C_{T,k}$可由实际滤波器的Chebyshev展开系数$\{a_{k,r}\}$直接计算，无需引入未知常数$C$。对GCN（$K_{\text{poly}}=1$，$c_0=0,c_1=1$，$T_1(x)=x$，$a_{1,1}=1$）而言，$C_{T,1}=1$，式~\eqref{eq:ch5-tau-grace}简化为$2(\sigma-1)\|X\|_F$。

从方法论上看，边数预算与谱近似质量之间通常存在权衡关系。经典谱稀疏化理论表明，对于加权图的组合拉普拉斯，在适当的重加权采样构造下可保留 $O(n\log n/\varepsilon^2)$ 条边（此结论不能由式~\eqref{eq:ch5-tau-grace}直接推出，仅作量级参照）。本文讨论的是基于归一化拉普拉斯的节点自适应稀疏化，并不与上述加权谱 sparsifier 构造完全等同；正式误差分析以式~\eqref{eq:ch5-budget-lagrange}的条件化界为准。

式~\eqref{eq:ch5-tau-grace}将稀疏图的谱相似性参数 $\sigma$ 与GNN输出误差直接联系起来；三维参数如何保证所需的 $\sigma$ 值，由下一小节在工程代理意义下衔接。


在 $\tau(i)\propto E_{\text{spectral}}^{(h)}(i)^{-1}$ 的设计下，局部扰动工程代理为 $\delta_i=O(E_{\text{threshold}}/E_{\text{spectral}}^{(h)}(i))$；高谱能量节点自动获得较小扰动。经算子范数聚合（以最低保留率 $\rho_{\min}$ 代理），可估计全局 $\sigma$ 满足
\begin{equation}
\sigma \lesssim 1 + C\cdot\frac{E_{\text{threshold}}}{E_{\text{threshold,ref}}}\cdot\alpha(G),\quad \alpha(G):=\frac{\sum_{i}(d_i/\sum_j d_j)/E_{\text{spectral}}^{(h)}(i)}{\hat{\lambda}_{\text{gap}}}
\label{eq:ch5-alphadef}
\end{equation}

其中 $C$ 为与图结构相关的常数，$\alpha(G)$ 为图结构代理量。该工程代理在局部→全局聚合步骤中尚未完全严格化，属近似间隙之一；其参数化意义在于为下述命题提供初始化依据。

\phantomsection\label{prop:ch5-kbar-bound}
在线性化传播假设（去除非线性激活）与附加扰动控制假设成立的条件下，对式~\eqref{eq:ch5-alphadef}由节点级局部扰动代理到每层谱相似性参数 $\sigma_l$ 的工程估计，并对第 $l$ 层滤波误差应用式~\eqref{eq:ch5-tau-grace}的结论，在线性化假设下逐层累加，可得 $L$ 层 GNN 输出误差的条件化上界：
\begin{equation}
\varepsilon_{\text{total}} \lesssim \sum_{l=1}^{L} 2(\sigma_l - 1)\cdot\left(\sum_{k=0}^{K_{\text{poly}}^{(l)}}|c_k^{(l)}|\,C_{T,k}\right)\cdot\|X\|_F,
\label{eq:ch5-budget-lagrange}
\end{equation}

其中 $\sigma_l$ 为第 $l$ 层对应的谱相似性参数，符号 $\lesssim$ 体现其工程代理地位——从节点级局部扰动代理 $\delta_i$ 到层级 $\sigma_l$ 上界的严格聚合步骤目前尚未完全建立（近似间隙四）。因此式~\eqref{eq:ch5-budget-lagrange}的定量预测仅在线性化传播与附加扰动控制假设同时成立时有效，应作为条件化工程参考界理解，而不宜视为封闭的端到端严格定理。

式~\eqref{eq:ch5-budget-lagrange}给出从三维设计参数 $(\tau_{\text{base}},\{p_l\})$ 到下游误差目标 $\varepsilon_{\text{total}}$ 的条件化联系，实践中可结合 Lanczos 代理量 $\widetilde{\sigma}_{\mathrm{proxy}}$（开销 $O(K_{\mathrm{Lanczos}}m)$）对 $\tau_{\text{base}}$ 候选值进行闭环验证；具体数值结果见第6章谱相似性验证部分。

% ---- 图~\ref{fig:ch5-ablation}~占位符 ----
\begin{figure}[htbp]
  \centering
        \includegraphics[width=1\linewidth]{5-3.pdf}
  % \setlength{\fboxsep}{1.2em}
  % \fbox{\parbox{0.88\textwidth}{\small\centering
  %   \textbf{【占位图~\ref{fig:ch5-ablation}~】}\\[0.6em]
  %   图题：$\tau_{\text{base}}$反向设计结果与经验值对比（多数据集）。横轴为理论推导的$\tau_{\text{base}}^{\text{practical}}$（对数刻度），纵轴为实测$\sigma$值；目标线$\sigma=1+\varepsilon_{\text{total}}=1.1$；颜色区分$\gamma=1/2$与$\gamma=1$两条实验线。
  % }}
  \caption{$\tau_{\text{base}}$反向设计结果与经验值的对比（多数据集）}
  \label{fig:ch5-ablation}
\end{figure}

式~\eqref{eq:ch5-tau-grace}与式~\eqref{eq:ch5-budget-lagrange}共同构成本章的误差分析依据，$\sigma$ 将作为第6章§6.3的验证度量之一；三类开销的效率分析见下一节。

\section{复杂度分析}
\label{sec:ch5-time}

第~\ref{sec:ch5-spectral}~节建立了精度保证；本节分析推理、训练与预处理三类开销，并说明线性推理假设的成立条件。三类开销须严格区分，“推理高效”主张仅针对固化后的单次稀疏矩阵乘。

就推理期复杂度而言，固化后的推理过程为
\begin{equation}
  \hat{H} = (\Theta^{\text{fixed}})^\top \tilde{\Phi}^{\text{cache}},
  \label{eq:ch5-prop31}
\end{equation}

仅遍历 $\mathrm{nnz}(\Theta^{\text{fixed}})=n\bar{k}$ 个非零项，复杂度 $O(n\bar{k}d)$，与层数 $L$ 完全无关。层级信息全部编码在 $\theta_i$ 的支撑集结构（各跳距预算配额 $\{k_i^{(l)}\}$）中。以缓存就绪聚合主项度量时，相对 GCN 的理论加速比为
\begin{equation}
  \text{Speedup}_{\text{agg}} = \frac{L\bar{d}}{\bar{k}},
  \label{eq:ch5-prop31b}
\end{equation}

该比值仅比较“聚合主项”，不含特征变换与缓存开销；在隐藏维度统一记为 $d$ 且缓存已就绪时成立（$\bar{d}:=m/n$ 为平均度，$m=\mathrm{nnz}(A)$）。

训练期与预处理方面，MSAS-GNN 的训练主要瓶颈在 Phase $\Theta$。若严格按分层 LARS 实现计，full-batch 口径下每轮复杂度可写为 $\sum_i\sum_{l=1}^{L} O(|\mathcal{R}_l(i)|^3+|\mathcal{R}_l(i)|^2d)$；若仅作候选集级别的同量级近似，可简写为 $\sum_i O(|C_i|^3+|C_i|^2d)$。Phase $W$ 每轮包含特征重算与稀疏聚合两部分，复杂度为 $O\!\bigl(T_W(nDd+n\bar{k}d)\bigr)$。一次性预处理包含谱分解 $O(mK_{\text{eig}})$、节点指标计算 $O(m+nK_{\text{eig}})$、候选集构建 $O(\sum_i|C_i|)$ 及预算分配 $O(nL)$，仅执行一次，不计入推理开销。各方法综合复杂度对比见表~\ref{tab:ch5-5-6}。

推理复杂度 $O(n\bar{k}d)$ 成立有三项前提条件。其一，$\bar{k}\ll\bar{d}$，即节点级稀疏预算远小于平均度，由预算设计控制；其二，单节点非零预算受 $k_{\max}$ 约束；其三，推理阶段图结构与缓存特征保持静态，适用于 Transductive 设定。第三项给出方法的使用边界，Inductive 场景的扩展留待后续工作。

% ---- 图~\ref{fig:ch5-complexity-boundary}~占位符 ----
\begin{figure}[htbp]
  \centering
        \includegraphics[width=1\linewidth]{5-4.pdf}
  % \setlength{\fboxsep}{1.2em}
  % \fbox{\parbox{0.88\textwidth}{\small\centering
  %   \textbf{【占位图~\ref{fig:ch5-complexity-boundary}~】}\\[0.6em]
  %   图题：第5章复杂度分解与适用边界示意图。表达内容：左侧以堆叠条形图比较预处理、Phase $\Theta$、Phase $W$ 与推理期四类开销；右侧以条件框列出“缓存已就绪”“$\bar{k}\ll\bar{d}$”“静态图/Transductive”三项适用前提，并用虚线标示超出条件后复杂度结论不再直接成立。 
  % }}
  \caption{复杂度分解与线性推理适用边界示意图}
  \label{fig:ch5-complexity-boundary}
\end{figure}

\begin{table}[htbp]
  \centering
  \small
  \setlength{\tabcolsep}{4pt}
  \begin{threeparttable}
  \caption{各方法推理与训练复杂度对比（固定图、全图一次前向/结构固化后查询口径；$m=\mathrm{nnz}(A)$，$m_{\text{sparse}}=n\bar{k}$，$K_{\text{eig}}\in[50,100]$视为常数）}
  \label{tab:ch5-5-6}
  \begin{tabular}{@{}>{\centering\arraybackslash}m{1.8cm}>{\centering\arraybackslash}m{2.2cm}>{\centering\arraybackslash}m{3.8cm}>{\centering\arraybackslash}m{1.8cm}>{\centering\arraybackslash}m{3.0cm}@{}}
    \toprule
    \textbf{方法} & \textbf{预处理（一次）} & \textbf{训练/epoch} & \textbf{推理} & \textbf{备注} \\
    \midrule
    GCN & — & $O(L \cdot m \cdot d)$ & $O(L \cdot m \cdot d)$ & 全图$L$层消息传递 \\[3pt]
    SDGNN & $\text{Cost}_{\text{cand}}$ & $\sum_i O(|C_i|^3+|C_i|^2 d)$ & $O(n\bar{k}d)$ & 训练后固化$\Theta$ \\[3pt]
    \textbf{MSAS-GNN} & $O(mK_{\text{eig}}+m+n)+\text{Cost}_{\text{cand}}+O(nL)$ & Phase$\Theta$：$\sum_i\sum_{l=1}^{L} O(|\mathcal{R}_l(i)|^3+|\mathcal{R}_l(i)|^2d)$（或候选集同量级近似）；Phase$W$：$O\!\bigl(T_W(nDd+n\bar{k}d)\bigr)$/轮 & $\boldsymbol{O(n\bar{k}d)}$ & 推理无$L$因子；训练瓶颈在Phase$\Theta$ \\[3pt]
    \bottomrule
  \end{tabular}
  \end{threeparttable}
\end{table}

\section{本章小结}
\label{sec:ch5-summary}

本章完成了从三维自适应参数到可执行算法的最后整合。第~\ref{sec:ch5-lars}~节以LARS为核心求解器，通过残差级联分层稀疏分解将 $\tau(i)$（节点正则系数）与 $k_i^{(l)}$（跳距预算配额）整合为端到端交替优化主循环（算法~\ref{alg:ch5-main}），训练收敛后通过算法~\ref{alg:ch5-fixed}固化为稀疏权重矩阵 $\Theta^{\text{fixed}}$，推理期退化为单次稀疏矩阵乘（式~\eqref{eq:ch5-tau-range}，复杂度 $O(n\bar{k}d)$）。第~\ref{sec:ch5-spectral}~节以 $\sigma$-谱相似性（式~\eqref{eq:ch5-tau-mono}）为出发点，给出多项式谱滤波近似误差界（式~\eqref{eq:ch5-tau-grace}）与 $L$ 层条件化联合误差界（式~\eqref{eq:ch5-budget-lagrange}），说明其适用条件；参数初始化结合 Lanczos 代理量的闭环验证已在上文说明，相关实测数据见第6章。第~\ref{sec:ch5-time}~节区分三类复杂度口径——推理 $O(n\bar{k}d)$（与 $L$ 无关）、训练 Phase $\Theta$ 量级与 SDGNN 相当、一次性预处理额外引入 $O(mK_{\text{eig}})$ 谱计算——并说明线性推理假设的三个成立条件（见第~\ref{sec:ch5-time}~节）。

本章已知局限如下：第4章自适应系数设计准则建立在若干工程近似之上，端到端误差界仅在线性化假设下成立；静态图条件将 Inductive 场景排除在外；LARS 训练开销仅可通过 mini-batch 工程近似缓解（具体实现见第6章实验设置），尚无根治方案。

本章核心输出为 $\Theta^{\text{fixed}}$（第6章效率实验主测对象）与 $\sigma$-谱相似性度量（第6章§6.3谱质量验证指标），两者共同支撑第6章的效率—精度全面评估。


