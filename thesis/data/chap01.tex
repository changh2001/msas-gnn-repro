% !TeX root = ../main.tex

\chapter{绪论}
\label{chap:intro}

在线图推理的核心矛盾在于：多层消息传递能够提升节点表示能力，却会因多跳邻域扩张显著推高推理代价。针对这一矛盾，SDGNN以单次稀疏分解替代逐层消息传递，为高效在线推理提供了可行思路，但其全局统一稀疏预算仍难以适应节点拓扑异质性。本文围绕这一局限展开研究，提出多尺度自适应稀疏图神经网络（MSAS-GNN）\label{idx:name:msasgnn}，而本章从研究背景与意义、国内外研究现状、研究内容与创新点、技术路线与论文组织结构四个层次说明本文的研究动机与整体安排，涉及贡献性符号与形式化结论统一在第~3--5~章建立。


\begin{figure}
  \centering
  \includegraphics[width=1\linewidth]{1-1.pdf}
  % \setlength{\fboxsep}{1.2em}
  % \fbox{\parbox{0.88\textwidth}{\small\centering
  %   \textbf{【图1.1 占位符，定稿前须替换为正式图】}\\[0.6em]
  %   图题：SDGNN全局统一稀疏预算局限与MSAS-GNN谱驱动自适应改进示意图\\[0.4em]
  %   表达内容：图采用左右对照结构。左侧以统一稀疏预算$\lambda_1$同时作用于高度数枢纽节点与低度数外围节点，突出“一刀切”分配带来的两类失配：前者易丢失关键邻居，后者保留较多冗余连接；右侧展示MSAS-GNN依据节点谱域特征进行差异化预算分配，在同配与异配两类图上分别保留更匹配的邻域信息。图中仅保留概念层面的模块框、节点示意与箭头关系，不出现后续章节的具体公式与算法编号。
  % }}
  \caption{SDGNN全局统一稀疏预算局限与MSAS-GNN谱驱动自适应改进示意图}
  \label{fig:ch1-limitation-overview}
\end{figure}


%% ============================================================
\section{研究背景与意义}
\label{sec:ch1-background}

在过去十多年的发展中，深度学习取得的成就主要限定在了计算机视觉、自然语言处理和音频分析领域上。这些领域上的数据和信息有着比较显著的特点。文本、图像、音频、视频的数据格式在形式上有着统一而规整的尺寸和维度，它们也被称作欧式结构（Euclidean Structure）或者网格结构（Grid Structure）数据。除此之外，现实生活中存在大量的非欧式结构的图数据，例如互联网、知识图谱、社交网络、蛋白质、化合物分子等。尽管深度学习在欧式结构数据上取得巨大的成功，但在图结构数据上，基于神经网络的深度学习表现得并不好。在图结构数据中，节点与节点之间的边连接可能是均匀分布的，也可能是不均匀的。节点与节点之间没有严格意义上的先后顺序。对于神经网络的输入端而言，这些数据没有固定的输入尺寸。在数学表达上，这些数据与欧式结构数据相比，每一个区块的特征矩阵维度都不是统一的\cite{JSJX202201003}。

图结构数据以节点和边刻画实体与关联，在社交网络、推荐系统、知识图谱与生物网络等领域普遍存在。图神经网络（Graph Neural Network，GNN）通过消息传递在图拓扑引导下递归聚合多跳邻域信息，在节点分类、链接预测等任务上表现出色，但$L$层GNN有效感受野规模约为$O(\bar{d}^L)$（$\bar{d}$为全图平均度），邻居收集与内存访问开销随层数快速膨胀。Hu等提出稀疏分解图神经网络（Sparse Decomposition of Graph Neural Networks，SDGNN\label{idx:name:sdgnn}），将推理重新表述为对教师GNN输出的单次稀疏分解\label{idx:theme:sparsedecomp}——推理阶段仅需执行$\hat{H}=\Theta^\top\tilde{\Phi}$，主项复杂度降至$O(n\bar{k}d)$（$\bar{k}$为平均非零权重数），以LARS求解器在全局统一正则系数$\lambda_{\mathrm{reg}}$下构建Lasso子问题，训练后固化$\Theta$\cite{Hu2024SDGNN}。

以同一$\lambda_{\mathrm{reg}}$约束所有节点稀疏预算\label{idx:theme:sparsebud}，隐含“节点最优稀疏度相同”的前提。真实图中节点拓扑特性通常存在显著差异\label{idx:theme:tophete}：枢纽节点往往承载更多全局结构信息，过激进稀疏化可能截断关键邻居；外围节点则更容易包含局部冗余连接，过保守稀疏化会削弱压缩收益。在结构更复杂、局部频谱参与方式差异更强的图上，统一参数尤其容易与节点真实的信息需求失配，如图~\ref{fig:ch1-limitation-overview}~所示。这一根本局限既在理论层面呼唤节点谱域特征与稀疏预算之间的量化关联，在实践层面也制约了推荐系统、实时风控等延迟敏感服务的部署灵活性。本文在保持$O(n\bar{k}d)$推理口径不变的前提下，尝试以图谱理论建立节点级自适应稀疏预算分配框架，使稀疏化方案在不同结构类型的静态图上均具备可追溯的设计依据，具体加速效果在第~6~章实验中给出。

\section{国内外研究现状}
\label{sec:ch1-related}

本节围绕与本文核心研究主线直接相关的三条文献脉络，概述国内外研究进展，为第~\ref{sec:ch1-contribution}~节的问题界定与第~2~章的系统综述形成清晰分工。

\subsection{图神经网络在线推理效率相关研究}
\label{subsec:ch1-gnn-efficiency}

Gilmer等\cite{Gilmer2017MPNN}\label{idx:name:gilmer}将多种图学习方法统一为消息传递神经网络（MPNN）框架，GCN\cite{Kipf2017GCN}\label{idx:name:gcn}、GAT\cite{Velickovic2018GAT}\label{idx:name:gat}和GIN\cite{Xu2019GIN}\label{idx:name:gin}分别从低通谱滤波、注意力加权和表达上界三个角度扩展了消息传递范式。这类方法的单轮前向开销通常与层数、边数和隐藏维度密切相关；以GCN为例，其全图前向主项复杂度可写为$O(L\cdot m\cdot d)$。在推荐系统、实时风控等毫秒级延迟约束场景中，多跳邻居收集形成大规模部署的根本瓶颈。随层数增加，过平滑\label{idx:theme:oversmooth}\cite{Li2018OverSmoothing}使节点表示趋于同质，过压缩\label{idx:theme:oversquash}\cite{Alon2021OverSquashing}使远端信息在瓶颈处丢失，实用层数因此通常限于2--3层。两类固有局限进一步强化了以稀疏分解替代逐层消息传递的技术动机。

\subsection{高效与稀疏图神经网络方法}
\label{subsec:ch1-sparse}

针对感受野扩张带来的推理瓶颈，现有方法沿三条路线演进。邻居采样路线（GraphSAGE\cite{Hamilton2017GraphSAGE}\label{idx:name:graphsage}、FastGCN\cite{Chen2018FastGCN}\label{idx:name:fastgcn}、GraphSAINT\cite{Zeng2020GraphSAINT}\label{idx:name:graphsaint}）在训练阶段有效控制计算量，但推理时仍需访问完整邻居，且采样策略固定，难以因节点差异化调整。预计算线性化路线（SGC\cite{Wu2019SGC}\label{idx:name:sgc}、APPNP\cite{Gasteiger2019APPNP}\label{idx:name:appnp}、SIGN\cite{Frasca2020SIGN}）大幅降低推理开销，但传播模式固化，无法逐节点自适应。稀疏分解路线中，SDGNN\cite{Hu2024SDGNN}将推理主项复杂度压至$O(n\bar{k}d)$（$\bar{k}\ll\bar{d}^L$），成为本文的直接基线；其关键局限在于全局统一正则系数$\lambda_{\mathrm{reg}}$对枢纽节点与外围节点施加相同稀疏压力，节点拓扑差异在频率域的系统性偏差难以消除，构成本文第~3--5~章三段式自适应稀疏机制设计的直接动机。

\subsection{复杂图结构学习与谱视角方法}
\label{subsec:ch1-heterophily}

已有研究表明，不同图结构的数据在有效判别频段上可能表现出明显差异：结构较平滑的引文网络往往更依赖低频聚合，而网页协作图等复杂结构数据更需要保留中高频信息\cite{Pei2020GeomGCN}。H2GCN\cite{Zhu2020H2GCN}\label{idx:name:h2gcn}、GPR-GNN\cite{Chien2021GPRGNN}\label{idx:name:gprgnn}、FAGCN\cite{Bo2021FAGCN}\label{idx:name:fagcn}分别从多阶聚合分离、自适应频率权重和高低通滤波组合角度突破统一低通限制，LINKX\cite{Lim2021LINKX}\label{idx:name:linkx}则通过分离特征变换与传播提高复杂图上的适应能力；但上述方法多采用全图共享的频率参数，节点级频率自适应能力仍然不足。谱间隙$\lambda_\text{gap}$\label{idx:theme:specgap}可刻画全局连通性，节点级频率响应还需结合局部谱统计量进一步量化；本文据此保留频率接口，但在正文主线中采用等权实例化以保持任务通用性，精细频率感知设计留待后续工作。

\section{研究内容与创新点}
\label{sec:ch1-contribution}

上述三条文献脉络表明：以SDGNN为代表的稀疏分解路线以全局统一参数主导节点稀疏预算，节点拓扑差异在频率域的影响尚未得到系统理论化；现有复杂结构图方法的频率参数大多停留于全图共享层面，节点级自适应能力不足。这一研究空白构成本文的直接动机。

本文的核心研究问题可表述为：在保持静态推理主项复杂度口径$O(n\bar{k}d)$不变的前提下，如何以可计算谱指标为依据，建立节点级差异化稀疏预算分配方案，使稀疏化方案在不同结构类型的静态图上均具备可追溯的设计依据，并在实验中得到系统验证。围绕这一目标，第~3--5~章的三段式方法链从问题形式化与指标提取、三维机制设计、算法整合与复杂度分析三个层次依次推进。

在方法设计层面\label{idx:innov:3dim}，本文在第~3~章建立了谱间隙、节点谱能量、归一化局部图熵与节点中心性四类可计算图复杂度量化指标，为后续节点自适应参数的构造提供统一的可计算依据，使参数设计从依赖经验调参转向基于图结构特征的量化分配。在此基础上，第~4~章沿频率、节点、跳距三个维度构建节点级自适应参数体系：频率维度保留频率接口并以等权实例化为默认配置（各频段权重均等，不引入标签统计量），在保持方法任务通用性的同时为后续精细频率感知设计预留接口；节点维度依据谱能量与邻居有效信息量的正相关关系设计节点级自适应正则系数；跳距维度依据多跳传播信息逐层衰减的经验规律设计分层预算分配策略。三维参数在推理阶段均固化为单次稀疏矩阵乘形式，推理主项复杂度维持$O(n\bar{k}d)$的线性量级。第~5~章完成LARS端到端整合与复杂度分析。

在实验验证层面，本文在六个静态节点分类基准数据集上的测试表明，MSAS-GNN总体优于基线SDGNN，在引文网络、网页图与大规模图场景下均取得稳定改善，节点级自适应正则系数与分层跳距预算发挥了可观作用；相对消息传递基线在ogbn-arxiv上实现约13.6倍推理加速，四数据集平均约8.0倍，显存降低约74.8\%；全部结果通过Wilcoxon符号秩检验的显著性验证，具体见第~6~章。

\section{技术路线与论文组织结构}
\label{sec:ch1-structure}

如图~\ref{fig:ch1-tech-route}~所示，全文七章可归为四个功能层次：第~1--2~章建立研究背景与理论基础；第~3--5~章构成核心方法链，按“问题定义$\rightarrow$三维机制设计$\rightarrow$算法理论整合”三个层次递进，以$O(n\bar{k}d)$推理口径为共同约束；第~6~章开展系统实验评估；第~7~章进行全文总结与展望。各章功能定位如下：第~1~章建立研究动机与问题框架；第~2~章围绕三条文献脉络建立基础理论；第~3~章完成图学习基本设定与四类指标提取；第~4~章设计三维节点级自适应参数体系；第~5~章完成算法整合、误差分析与复杂度分析；第~6~章在多类节点分类数据集上开展主实验、消融与效率评估；第~7~章进行研究总结与未来工作展望。附录A对各章核心设计公式的理论依据进行简要说明，附录B补充完整算法流程，附录C提供实验细节。

\begin{figure}[htbp]
  \centering
    \includegraphics[width=1\linewidth]{1-2.pdf}
  % \fbox{\parbox{0.90\linewidth}{
  % 图1.2占位内容：全文技术路线与章节接口图。上层为第~1--2~章的研究背景与理论基础，
  % 中层为第~3--5~章三段式方法链（问题形式化与指标体系$\rightarrow$三维自适应机制设计$\rightarrow$算法整合、理论保证与复杂度分析），
  % 下层为第~6~章实验评估与第~7~章结论展望。箭头仅标示章间输入输出关系，不出现具体公式与算法编号。}}
  \caption{本文技术路线与章节接口示意图}
  \label{fig:ch1-tech-route}
\end{figure}

\section{本章小结}
\label{sec:ch1-summary}

本章的任务不是提前展开方法细节，而是把后续研究的起点交代清楚。围绕在线图推理中的效率—表达能力矛盾，本章首先以SDGNN为切入点，指出全局统一稀疏预算难以回应节点拓扑差异这一核心局限；随后沿推理效率、高效稀疏化与复杂图结构学习三条文献脉络收束出本文的关键研究空白，即节点级自适应稀疏预算尚缺少统一的谱论依据；在此基础上，本文将研究问题限定为在保持$O(n\bar{k}d)$静态推理主项口径下构建谱驱动的三维自适应参数体系。下一章将进一步整理后续方法章节反复调用的基础概念、代表方法与研究空白。
